# Progress

現在地のサマリ。新セッションは最初にここを見る。

## 現在のフェーズ

**P1 完了 → P2（MVP / `mvp/`）。** 作るアプリ = **Oracle**（あなたの次の一手を読み当て「人はランダムになれない」を体験させる Web アプリ）。

## フェーズ進行

- [x] P0 ボイラープレート初期化
- [x] P1 アイデア選定（Oracle に決定）
- [ ] P2 MVP（使い捨て / `mvp/`）
- [ ] P3 app-design（詳細設計）
- [ ] P4 機能仕様
- [ ] P5 本実装（`app/` / 0 から）
- [ ] P6 リリース準備

各フェーズの移行ゲートは `03-ROADMAP.md`。**現フェーズのゲートが全チェックになるまで次へ進まない。**

## 現フェーズのゲート（P2 → P3）

- [ ] `mvp/` で最小フロント＋中核機能が動く（手動確認 or smoke 緑）
- [ ] 中核体験に対して以降の調整が不要であり、製作者がその質に納得している
- [ ] 中核体験が確定。実験・学びを `docs/mvp-design/` に記録し、確定した学びを `docs/progress/` / `app-design` へ昇格
- [ ] 本実装(P5)は `app/` で 0 から作る前提を確認（MVP コードは引き継がない）

## 進行中

- P2 MVP を `mvp/`（Vite + React 19 + TS）に実装予定。中核体験「← / → をランダムに押すあなたを Oracle が押下前に当て続ける」の成立を確認する。
- 予測モデル（頻度 / 1次マルコフ / n-gram バックオフ）を切替可能に保つ。

## 次にやること

1. `mvp/` を Vite で初期化し、Oracle の最小実装（入力→予測→ヒット表示＋精度＋モデル切替）。
2. 手動確認で「読まれて驚く」が成立するかを検証 → 学びを mvp-design / progress に記録。
3. P2 ゲートを満たしたら P3 app-design でスタックを 0 から確定し `04-ARCHITECTURE.md` を pin。

## レビュー待ち

なし。

<!-- 一方通行（不可逆／高コスト）な決定でユーザ確認が要るものだけ列挙する。
     引き返せる決定は載せず、根拠を progress に残して進める（02-GUIDELINES「レビューゲート」）。
### <対象名>（目安: 例 20-30 分）
- 対象: <ファイルパス>
- なぜ一方通行か: <後から変える高コスト/不可逆な点>
- 見てほしい点（重要度順）: 1. <一番効く判断> 2. <次に効く点>
- 確認OKなら `🔍 reviewed` 化（ユーザ確認なしには付けない）
-->

## 未解決 issue

0 件。

## 参照

- [Roadmap（フェーズとゲート）](03-ROADMAP.md)
- [Progress logs](progress/) / [Ideas](ideas/) / [App Design](app-design/) / [Specs](specs/)
