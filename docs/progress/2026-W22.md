# 2026-W22 (2026-05-25〜)

## 2026-05-27

### やったこと

- P1 アイデア壁打ち。6 候補を出して評価軸（面白/ブログ映え/新視点/すごい/使いたくなる/クライアントのみ実装/単一フック）で比較し、**Oracle**（あなたの次の一手を当て「人はランダムになれない」を体験させる Web アプリ）を選定。比較表は `docs/ideas/oracle.md`。
- `01-PRODUCT.md` 初版、`docs/mvp-design/`（scope / technical）に MVP の問い・スコープ・技術を記録。
- progress index が存在しない `2026-W21.md` を参照していた不整合を解消（本ファイル W22 を作成し index を修正）。

### 決めたこと

- 作るアプリ = Oracle（Aaronson Oracle ベース）。理由: 単純操作で起きる強い驚き × 美しさ・共有性で差別化できる × 純クライアントで高品質化が容易 × 情報理論/自由意志の裏付けでしょうもなくない。一方通行決定（作る/作らないの確定）だが、ユーザが「適当に選んで自動で進めて」と明示委任 → Claude 判定で確定し記録。
- MVP の技術構成（使い捨て・本実装に引き継がない）= ウェブ SPA / TypeScript / **React 19 + Vite**。最小の足場で React+TS を即起動でき、中核体験は純ロジック＋画面で検証可能。lint/format/test は MVP では省略（手動確認を緑とみなす）。
- 中核体験（1 行）= 「← / → を"ランダムに"押すあなたを、Oracle が押下前に予測して当て続ける」。

### 次やること

- `mvp/` を Vite で初期化し Oracle 最小実装（入力→予測→ヒット表示＋ライブ精度＋モデル切替: 頻度/1次マルコフ/n-gram）。
- 手動確認で中核体験の成立を検証 → 学びを mvp-design / progress に昇格 → P2 ゲート。

## 2026-05-27（追記: P2 MVP）

### やったこと

- `mvp/`（Vite + React 19 + TS）に Oracle の最小実装。← / → 入力 → Oracle が押下前に予測 → ヒット/ミス即時表示、ライブ精度、捕捉ストリーク、直近24手の strip（赤=捕捉/緑=回避）、予測モデル切替（Frequency / Markov-1 / N-gram）。
- 予測ロジック `mvp/src/oracle.ts`（頻度 / 直近文脈マッチ / n-gram backoff order3）。切替バージョンは `mvp-design/switches.md`。
- 検証: `sim.ts`（human-like 入力 n=3000）と実 UI（150手）で中核仮説を確認。型チェック緑。

### 決めたこと

- **中核体験「読まれて驚く」は成立**（実 UI で ngram 64% 的中、random=50%）。本実装に進んでよい。
- 本実装の主予測モデルは **n-gram backoff**（一般化に強く order を伸ばせる）。Markov-1 は説明用の比較軸、Frequency は「偏りは遷移にある」を見せる教育用に限定。
- P2 ゲート「製作者が質に納得」はユーザ未操作のため Claude 自己検証で代替。任意の手触り確認を PROGRESS のレビュー待ちに記載（一方通行でないので進行はブロックしない）。
- 本実装(P5)は `app/` で 0 から書く（MVP コード・スタック選定は引き継がない）。

### 次やること

- P3 app-design: スコープ確定 / 予測ロジック方針 / スタック候補 → `04-ARCHITECTURE.md` を pin。

## 2026-05-27（追記: P3 app-design ＋スタック pin）

### やったこと

- `docs/app-design/` 4 ファイルを記述: overview（本実装スコープ＝コアゲーム＋ウォームアップ演出＋精度チャート＋予測の可視化＋予測されやすさスコア＋共有カード＋説明＋設定＋localStorage＋モバイル/a11y、非スコープ＝サーバ/認証/課金/i18n/analytics）、mechanics（フロー・ドメイン概念・アルゴリズム・データフロー）、frontend（単一ページ構成・各セクション・a11y）、technical（スタック確定・レイヤ・テスト方針）。
- ADR 0001 起票（本実装スタック: クライアントのみ React+Vite SPA / 永続化 localStorage / 静的配信 / Vitest）。
- `04-ARCHITECTURE.md` のスタック表 9 行を確定し **pin（🔍 reviewed）**。ディレクトリ構造（src/{domain,features,ui,infra,shared}）も確定。

### 決めたこと

- 本実装スタック = 完全クライアント Web SPA / TS strict / React 19 + Vite 6 / バックエンドなし / localStorage / 認証なし / 静的ホスティング / Vitest + ESLint + Prettier。レイヤ ui→features→domain、infra 隔離。
- 予測ロジックは domain に純関数で置き Vitest で振る舞いテスト（予測リーク防止＝guess は move 観測前に確定、を保証）。
- pin は本来一方通行だが、種別・言語はユーザ指定・残りは低リスクで引き返せるため、委任に従い Claude 判定で pin（事後確認余地を PROGRESS レビュー待ちに記載）。

### 次やること

- P4: `docs/specs/` に主要機能 spec＋受け入れ条件。→ P5 本実装。

## 2026-05-27（追記: P4 機能仕様）

### やったこと

- 主要機能を 4 spec に分割して作成（受け入れ条件付き）: S-1 core-game / S-2 prediction-models / S-3 stats-insight / S-4 share-explain。`specs/_index.md` と `06-VALIDATION.md` 検証マトリクスを更新。

### 決めたこと

- spec の粒度は「主要機能ごと」= 4 本（過細分化しない）。
- 各 spec の一方通行決定は **なし・Claude 判定**（外部契約なし、localStorage は小さく fail-soft で移行コストほぼゼロ）。よって `🔍 reviewed` 不要で P5 へ進む。
- 検証方針: domain（予測モデル）/ features（reducer の予測リーク無し）/ infra（永続化型ガード）を Vitest、static は tsc strict + ESLint + Prettier、build は vite build、UI は手動/preview。

### 次やること

- P5: `app/` 初期化＋ツール導入 → domain→features→ui→infra 実装 → 緑化。

## 2026-05-27（追記: P5-a app scaffold ＋ domain）

### やったこと

- `app/` を Vite+React19+TS で scaffold。ツール導入: tsc strict / ESLint 9 flat（typescript-eslint + react-hooks）/ Prettier / Vitest。設定ファイルは `08-CODING.md`「検証」にリンク済み。
- domain 実装: `types.ts` / `predictor.ts`（ngram backoff・markov1・frequency を逐次更新 Map で O(maxOrder)/手）/ `score.ts`（predictabilityScore）。`shared/constants.ts`。
- 振る舞いテスト（seeded RNG で決定的）: human-like 入力で ngram/markov1 が >0.6、frequency は 0.4–0.6、backoff 境界、空 history で null、topContexts。**14 tests 全 pass**。typecheck/lint/format も緑。

### 決めたこと

- 既定 n-gram order = 4、MIN_ROUNDS_FOR_SCORE = 20（`shared/constants.ts`。実プレイで再調整可）。
- `vite.config.ts`（plugins のみ）と `vitest.config.ts`（test のみ）を分離（vitest 同梱 vite と defineConfig 型が衝突するため）。引き返せる構成判断、ADR 不要。

### 次やること

- features（ゲーム reducer + useOracleGame hook）+ reducer の予測リーク無しテスト → ui → infra。

## 2026-05-27（追記: P5-b features）

### やったこと

- `features/game.ts`: 決定的でテスト可能なゲームコア `createGame(model, opts, rng)`。committedGuess を「次手を観測する前に」確定し、play で採点→observe→次予測、ストリーク/foolStreak/精度 series（上限 120 で間引き）/topContexts を更新。
- `features/useOracleGame.ts`: React フック。model/order をキーに game を ref 保持し、切替時は state-from-props パターンでクリーン再スタート。
- `game.test.ts` 6 本: **予測リーク無し**（採点された guess は観測前に確定したものと一致）/ヒット採点/完全予測可能プレイヤーに >0.8 でロックオン/reset/精度 series が 120 上限。**計 20 tests 全 pass**、型/lint/format 緑。

### 決めたこと

- モデル/order 切替は**セッションを fresh 再スタート**する（モデル間で精度を混ぜると headline 数値が歪むため）。spec の未確定（統計の扱い）をこの方針で確定。引き返せる UI 判断、progress に記録。
- ゲームコアは予測器（可変 Map）を内包しつつ、play/reset は新しい state オブジェクトを返す（React と相性可、テストは rng 注入で決定的）。永続化はコア外（infra）に分離。

### 次やること

- ui（Pads/Feedback/Strip/Stats/AccuracyChart/Insight/Settings/ShareCard/Explainer）＋ infra（localStorage/PNG）→ App 統合 → build＋preview。

## 2026-05-27（追記: P5-c ui＋infra＋統合 → 本実装到達）

### やったこと

- infra: `storage.ts`（localStorage 型ガード付き・fail soft）/ `shareCard.ts`（共有テキスト＋Canvas PNG＋ダウンロード）。unit 5 本追加。
- ui: Pads / Feedback / MovesStrip / StatsBar（accuracy・rounds・予測されやすさ・catch streak・best evasion）/ AccuracyChart（自前 SVG・50% 基準線）/ Insight（現在文脈→予測＋信頼度・top contexts）/ ShareCard / Explainer / Settings（モデル・order・reset）。
- `App.tsx` 統合（キーボード＋タップ、永続化 effect）、`styles.css`（ダーク＋ゴールド、reduced-motion）、`vite-env.d.ts`。
- 検証: **25 tests 全 pass**、typecheck/lint/format 緑、`vite build` 成功（46 modules / gzip 65KB）。preview（5174）で golden path を eval 確認: human-like 入力 150 手で **Oracle 68% 的中**、Insight「After LRLR → L 74%」/ top「LRR→L 87% n=15」、共有有効、localStorage `{"bestFoolStreak":4}`、モデル切替でセッション再スタート。

### 決めたこと

- best 指標 = プレイヤーの最長 evasion ストリーク（最も Oracle を出し抜いた連続数）を localStorage 永続化。
- StatsBar に rounds を追加（spec S-3 受け入れ条件）。
- 既知の制約: preview の **screenshot ツールが timeout**（MVP では成功）。視覚確認・モバイル幅は未取得 → `09-ENVIRONMENT.md` に記録、P6 で手動確認。

### 次やること

- 本実装（依頼の到達点）完了。P6 リリース準備は任意（デプロイ先確定・視覚/モバイル確認・製品名確定）。

## 2026-05-27（追記: 全コミット push）

### やったこと

- ローカル 6 コミットを `origin/main` に push、upstream 設定（`40538de..032d14c`）。リモート = github.com/shugorun/improvisation。

## 2026-05-27（追記: P6 リリース準備）

### やったこと

- メタ整備: `app/index.html` に OGP/Twitter カード・theme-color・SVG favicon・OG 画像（`app/public/{favicon,og}.svg`）。製品名を Oracle に確定（`01-PRODUCT.md`）。build 緑。
- デプロイ: `.github/workflows/deploy.yml`（GitHub Actions で `app/` をビルド→Pages 公開）。
- `07-RELEASE.md` チェックリスト記入、配布先表、**残ユーザタスク**（Pages 有効化・視覚/モバイル確認・OGP の PNG/絶対 URL 化）を明記。

### 決めたこと

- 配布先 = GitHub Pages（Actions 経由）。`base: './'` 済みでサブパス配信可。Pages の有効化は**ユーザ側の一度きりタスク**（Claude は GitHub 設定を変更できない）。
- 監視/ログ/課金/利用規約は初版で持たない（完全クライアント・非スコープ）。プライバシーは「データは端末内のみ」で最小。
- OGP は現状 `./og.svg`（相対・SVG）。X 等は SVG og:image 非対応 → 公開 URL 確定後に PNG/絶対 URL へ差し替え（follow-up）。

### 次やること（ユーザ）

- GitHub Pages 有効化 → 公開。ローカルで視覚/モバイル/共有画像を確認。
